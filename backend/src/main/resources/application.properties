spring.application.name=passthesalt-backend
server.port=8080

# Enable CORS for frontend development
server.servlet.context-path=/

spring.profiles.active=h2
spring.jpa.hibernate.ddl-auto=update

# Clerk JWT verification
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://wanted-kite-82.clerk.accounts.dev

# Security mode:
# - default (no profile): security temporarily disabled for local dev
# - secure profile: enable auth + @PreAuthorize checks
# spring.profiles.active=secure

