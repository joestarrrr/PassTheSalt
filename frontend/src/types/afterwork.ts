export type AfterworkEventResponse = {
  eventId: number
  title: string
  location: string
  eventDate: string
  createdByUserId: number
}

export type AfterworkEventItem = {
  id: number
  name: string
  date: string
  rawDate: string
  status: string
}
